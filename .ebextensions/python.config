option_settings:
  aws:elasticbeanstalk:container:python:
    WSGIPath: dashboard:server  # Change this to point to your Flask app instance

container_commands:
  01_start_gunicorn:
    command: "gunicorn --bind 0.0.0.0:8080 dashboard:server"  # Adjust to match your entry point
